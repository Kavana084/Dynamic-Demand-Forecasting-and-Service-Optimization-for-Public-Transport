version: '3.8'

services:
  api:
    build: .
    container_name: transit_optimization_api
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - ENVIRONMENT=production
    restart: unless-stopped
